%h4.subtitle ...my cousin works for Bit.ly

%span.page_top_link= link_to image_tag('back.png') + ' list all articles', articles_path, :title => "Go to the article list" %></span>

%h2
  Editing
  @article.title

= render 'form'